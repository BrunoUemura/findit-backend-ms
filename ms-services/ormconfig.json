{
  "type": "postgres",
  "url": "postgresql://postgres:postgres@host.docker.internal:25432/findit_service",
  "ssl": false,
  "entities": ["src/models/**/*.ts"],
  "migrations": ["src/database/migrations/**/*.ts"],
  "cli": {
    "migrationsDir": "src/database/migrations"
  }
}
